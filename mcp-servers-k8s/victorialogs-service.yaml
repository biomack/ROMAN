apiVersion: v1
kind: Service
metadata:
  name: mcp-victorialogs
  namespace: keep-dev
  labels:
    app: mcp-victorialogs
spec:
  selector:
    app: mcp-victorialogs
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP