apiVersion: v1
kind: Service
metadata:
  name: mcp-victoriametrics
  namespace: keep-dev
  labels:
    app: mcp-victoriametrics
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: mcp-victoriametrics
  type: ClusterIP